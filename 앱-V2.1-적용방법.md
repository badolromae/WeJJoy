# WeJJoy 앱 V2.1 — 적용 방법

이 묶음은 **누적본**입니다. 지금 저장소가 1.24든 2.0이든, 이걸 올리면 바로 **2.1**이 됩니다.

## V2.1에서 바뀐/더해진 점
- **일기 보기(전체화면) 화면 추가** — 목록/검색/달력에서 일기를 누르면, 바로 수정화면이 아니라
  **제목·내용·기분·중요도**를 보여주는 전체화면이 먼저 뜹니다. 내용 속 이모티콘은 **큼직한 그림**으로 표시.
  우측 상단 **연필(✏️) 아이콘**을 누르면 수정화면으로 들어갑니다.
- **달력 공휴일·기념일 글씨 잘림 수정** — 칸을 살짝 키우고 정리해서 이름이 다 보이게.
- (V2 기능 포함) 이모티콘 60종 내장, 편집기 이모티콘 고르기(대표/제목/내용), 목록·달력 이모티콘,
  제목·내용 글 속 이모티콘, 웹과 실시간 공유.
- **설정 버전: WeJJoy 버전 2.1**
- 기존 일기 그대로 보존 (DB 자동 마이그레이션).

## 적용 방법 (제일 쉬운 방법)
1. 압축을 풉니다 → `app` 폴더가 있습니다.
2. 깃허브 `badolromae/WeJJoy` ▸ **Add file ▸ Upload files** ▸ `app` 폴더 **통째로 드래그** ▸ **Commit changes**.
   - 같은 경로 파일은 새 내용으로 바뀌고, 새 파일(보기화면·이모티콘 등)은 추가됩니다. 다른 파일은 안 건드립니다.
3. **Actions ▸ Build APK** 가 자동으로 돌아 **버전 2.1 APK** 생성 → Artifacts의 `MyDiary-APK` 내려받아
   **덮어쓰기 설치**. (앱을 지우지 말고 항상 덮어쓰기 설치 — 기존 일기 유지)

## 확인
- 일기를 누르면 전체화면 보기 → 우측 상단 연필로 수정.
- 내용에 넣은 이모티콘이 보기화면에서 크게 보이면 성공.
- 달력의 공휴일/기념일 이름이 안 잘리고 다 보이면 성공.
- 설정 맨 아래 "WeJJoy 버전 2.1".
