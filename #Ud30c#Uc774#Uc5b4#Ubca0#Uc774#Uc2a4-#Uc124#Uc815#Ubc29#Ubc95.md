# 파이어베이스(Firebase) 설정 방법 — WeJJoy 공유 서버 만들기

> 이 문서 하나만 그대로 따라 하시면 됩니다. 개발 지식은 필요 없습니다.
> 전부 **무료 요금제(Spark)** 로 충분합니다. 카드 등록도 필요 없습니다.
> 처음이면 **20~30분** 정도 걸립니다. 한 번만 하면 끝입니다.

---

## 먼저 알아두실 것

- 이 설정은 **관리자(주 사용자) 한 사람만** 하면 됩니다.
- 공유자(가족·친구)는 아무것도 안 해도 됩니다. **초대 코드만 받아서 입력**하면 끝입니다.
- 설정을 안 하고 그냥 앱을 쓰셔도 됩니다. 그 경우 **공유 기능만 꺼진 채** 평소 다이어리로 잘 동작합니다.
- 여기서 얻는 값 4개를 `FirebaseConfig.kt` 파일에 넣고 다시 빌드하면 공유 기능이 켜집니다.

---

## 1단계 — 파이어베이스 프로젝트 만들기

1. 컴퓨터 브라우저에서 **https://console.firebase.google.com** 접속
2. 평소 쓰시는 **구글 계정으로 로그인**
3. **`프로젝트 만들기`** 클릭
4. 프로젝트 이름에 **`wejjoy`** 입력 → `계속`
   - 이름 밑에 `wejjoy-1a2b3` 같은 아이디가 자동으로 생깁니다. **이게 나중에 쓸 프로젝트 ID입니다.** 눈여겨 봐두세요.
5. **Google 애널리틱스** 화면이 나오면 **끄기(사용 안 함)** 로 두고 `계속`
   - 켜도 상관없지만, 필요 없어서 끄는 게 간단합니다.
6. `프로젝트 만들기` → 잠시 기다렸다가 `계속`

---

## 2단계 — 안드로이드 앱 등록하기

1. 프로젝트 첫 화면 가운데에 있는 **안드로이드 모양 아이콘** 클릭
   - 안 보이면 왼쪽 위 `프로젝트 개요` 옆 **톱니바퀴 ▸ 프로젝트 설정** ▸ 아래로 내려서 `앱 추가` ▸ 안드로이드
2. **Android 패키지 이름** 칸에 아래를 **정확히 그대로** 입력 (오타 나면 나중에 연결 안 됩니다)

   ```
   com.jooshin.wejjoy
   ```

3. 앱 닉네임은 `WeJJoy` 라고 적으시면 됩니다. (안 적어도 됨)
4. **디버그 서명 인증서 SHA-1** 은 **비워두세요.** 필요 없습니다.
5. `앱 등록` 클릭
6. 다음 화면에서 **`google-services.json` 파일을 받으라고 나오는데, 받지 않으셔도 됩니다.**
   그냥 `다음` → `다음` → `콘솔로 이동` 을 눌러 빠져나오세요.
   - WeJJoy 앱은 그 파일 대신 값 4개를 직접 넣는 방식이라 파일이 필요 없습니다.

---

## 3단계 — 익명 로그인 켜기 (중요)

공유자들이 아이디·비밀번호를 만들지 않고도 쓸 수 있게 하는 설정입니다.

1. 왼쪽 메뉴에서 **`빌드` ▸ `Authentication`** 클릭
2. **`시작하기`** 클릭
3. 로그인 방법 목록에서 맨 아래쪽 **`익명`** 클릭
4. 오른쪽 스위치를 **사용 설정(파란색)** 으로 켜고 **`저장`**

> 이걸 안 켜면 앱에서 "서버에 연결하지 못했습니다" 라고 나옵니다.

---

## 4단계 — 실시간 데이터베이스 만들기

1. 왼쪽 메뉴에서 **`빌드` ▸ `Realtime Database`** 클릭
   - ⚠️ **`Firestore Database` 가 아닙니다.** 반드시 **Realtime Database** 를 고르세요.
2. **`데이터베이스 만들기`** 클릭
3. 위치는 **`싱가포르 (asia-southeast1)`** 를 고르세요. (한국에서 가장 빠릅니다) → `다음`
4. 보안 규칙은 **`잠금 모드에서 시작`** 을 고르고 `사용 설정`
   - 다음 단계에서 올바른 규칙으로 바꿀 겁니다.
5. 만들어지면 화면 위쪽에 이런 주소가 보입니다. **이게 데이터베이스 주소입니다.**

   ```
   https://wejjoy-1a2b3-default-rtdb.asia-southeast1.firebasedatabase.app
   ```

---

## 5단계 — 보안 규칙 넣기 (중요)

아무나 남의 일기를 못 보게 막는 설정입니다. **꼭 하셔야 합니다.**

1. `Realtime Database` 화면 위쪽 탭에서 **`규칙`** 클릭
2. 안에 있는 내용을 **전부 지우고**, 아래 내용을 **통째로 복사해서 붙여넣기**
3. **`게시`** 버튼 클릭

```json
{
  "rules": {
    "groups": {
      "$code": {
        ".write": "auth != null && !data.exists() && newData.child('meta').child('ownerUid').val() === auth.uid",

        "meta": {
          ".read": "auth != null",
          ".write": "auth != null && data.child('ownerUid').val() === auth.uid",
          ".validate": "newData.hasChildren(['ownerUid', 'createdAt'])"
        },

        "members": {
          ".read": "auth != null && (data.child(auth.uid).exists() || root.child('groups').child($code).child('meta').child('ownerUid').val() === auth.uid)",
          "$uid": {
            ".write": "auth != null && (($uid === auth.uid && !root.child('groups').child($code).child('banned').child(auth.uid).exists()) || root.child('groups').child($code).child('meta').child('ownerUid').val() === auth.uid)",
            ".validate": "newData.hasChildren(['nick', 'role', 'joinedAt']) && newData.child('nick').isString() && newData.child('nick').val().length <= 40"
          }
        },

        "banned": {
          ".read": "auth != null",
          "$uid": {
            ".write": "auth != null && root.child('groups').child($code).child('meta').child('ownerUid').val() === auth.uid",
            ".validate": "newData.hasChildren(['at'])"
          }
        },

        "entries": {
          ".read": "auth != null && root.child('groups').child($code).child('members').child(auth.uid).exists()",
          ".write": "auth != null && root.child('groups').child($code).child('members').child(auth.uid).exists()",
          "$entryUid": {
            ".validate": "newData.hasChildren(['uid', 'updatedAt'])"
          }
        }
      }
    }
  }
}
```

이 규칙이 하는 일:

| 하는 일 | 누가 할 수 있나 |
|---|---|
| 그룹 새로 만들기 | 로그인한 사람 누구나 (자기가 관리자가 됨) |
| 초대 코드가 맞는지 확인 | 로그인한 사람 누구나 |
| 그룹에 참여하기 | 초대 코드를 아는 사람 |
| 일기 읽기·쓰기 | **그 그룹의 구성원만** |
| 공유자 내보내기 / 다시 허용 | **관리자만** |
| 그룹에서 나가기 | 본인만 |
| 내보내진 사람이 코드로 재입장 | **막힘** (관리자가 '다시 허용' 해야 가능) |

> 초대 코드는 `WJ-` + 6자리라 경우의 수가 **10억 가지가 넘습니다.** 남이 찍어서 맞힐 수 없습니다.
> 그래도 코드는 아무 데나 공개로 올리지 마시고, 알려주고 싶은 사람에게만 보내주세요.

---

## 6단계 — 값 4개 찾아서 앱에 넣기

### 6-1. 값 4개 찾기

1. 왼쪽 위 **톱니바퀴 ▸ `프로젝트 설정`** 클릭
2. **`일반`** 탭 아래로 내리면 **`내 앱`** 에 아까 등록한 안드로이드 앱이 보입니다.
3. 거기서 아래 3개를 찾습니다.

| 이름 | 화면에 나오는 이름 | 예시 |
|---|---|---|
| ① 프로젝트 ID | **프로젝트 ID** | `wejjoy-1a2b3` |
| ② 앱 ID | **앱 ID** | `1:123456789012:android:abcdef1234567890` |
| ③ 웹 API 키 | **웹 API 키** | `AIzaSyD-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` |

4. 마지막 하나는 4단계에서 본 데이터베이스 주소입니다.
   (`빌드 ▸ Realtime Database` 화면 위쪽에 있습니다)

| 이름 | 예시 |
|---|---|
| ④ 데이터베이스 주소 | `https://wejjoy-1a2b3-default-rtdb.asia-southeast1.firebasedatabase.app` |

### 6-2. 깃허브에서 파일 고치기

1. 깃허브에서 **`diary-group`** 저장소를 엽니다.
2. 이 순서로 폴더를 눌러 들어갑니다.

   ```
   app ▸ src ▸ main ▸ java ▸ com ▸ jooshin ▸ diary ▸ sync ▸ FirebaseConfig.kt
   ```

3. 파일이 열리면 오른쪽 위 **연필 아이콘(✏️ Edit this file)** 클릭
4. 아래 4줄의 **따옴표 `""` 사이에** 값을 붙여넣습니다.

   고치기 전:
   ```kotlin
   const val PROJECT_ID = ""
   const val APPLICATION_ID = ""
   const val API_KEY = ""
   const val DATABASE_URL = ""
   ```

   고친 뒤 (예시 — 본인 값으로 바꾸세요):
   ```kotlin
   const val PROJECT_ID = "wejjoy-1a2b3"
   const val APPLICATION_ID = "1:123456789012:android:abcdef1234567890"
   const val API_KEY = "AIzaSyD-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
   const val DATABASE_URL = "https://wejjoy-1a2b3-default-rtdb.asia-southeast1.firebasedatabase.app"
   ```

   ⚠️ 주의할 점
   - 따옴표 `"` 를 지우지 마세요. **따옴표 안쪽에만** 넣습니다.
   - 주소 끝에 **`/` 를 붙이지 마세요.**
   - 앞뒤에 빈칸이 들어가지 않게 하세요.

5. 오른쪽 위 **`Commit changes...`** ▸ **`Commit changes`** 클릭
6. 저장하면 **깃허브 액션이 자동으로 새 APK를 만들기 시작합니다.** (5~10분)

---

## 7단계 — 앱에서 그룹 만들고 초대하기

### 관리자(나)

1. 새로 나온 APK를 설치하고 앱을 엽니다.
2. **설정(톱니바퀴) ▸ 공유 그룹** 으로 들어갑니다.
3. **내 별명** 을 입력합니다. (예: `아빠`)
4. **`그룹 만들기`** 를 누릅니다.
5. `WJ-ABC234` 같은 **초대 코드**가 나옵니다.
6. **`코드 공유하기`** 를 눌러 카카오톡·문자로 상대에게 보냅니다.

### 공유자(상대방)

1. 같은 APK를 받아 설치합니다.
2. **설정 ▸ 공유 그룹** 으로 들어갑니다.
3. **내 별명** 을 입력합니다. (예: `엄마`)
4. **초대 코드** 칸에 받은 코드를 넣고 **`참여하기`** 를 누릅니다.
5. 끝입니다. 이제부터 서로의 일정이 **자동으로 실시간 공유**됩니다.

### 공유 해제하기 (관리자만)

- **설정 ▸ 공유 그룹 ▸ 공유자 목록** 에서 이름 옆 **`해제`** 를 누르면 그 사람이 그룹에서 빠집니다.
- 빠진 사람 폰에는 그때까지 받은 기록이 남지만, **새 내용은 더 이상 오지 않습니다.**
- 해제된 사람은 목록 아래에 **`내보냄`** 으로 남습니다. **초대 코드를 다시 넣어도 들어올 수 없습니다.**
- 잘못 눌렀으면 그 이름 옆 **`다시 허용`** 을 누르면 됩니다. 그러면 다시 참여할 수 있습니다.

---

## 8단계 — 사진도 같이 공유하기 (선택)

여기까지만 해도 글·시간·태그·중요도·기분은 다 공유됩니다. **사진까지 상대 폰에도 보이게** 하고 싶을 때만 이 단계를 추가로 하시면 됩니다. 안 하셔도 되고, 나중에 언제든 추가하셔도 됩니다.

> ⚠️ 사진 공유는 **Blaze(종량제) 요금제**로 바꿔야 켤 수 있습니다. 신용카드 등록이 필요합니다.
> 다만 매달 일정 용량까지는 **무료 사용량(No-cost tier)** 이 그대로 적용돼서, 가족·소규모로 쓰는 정도면 실제 청구 금액은 **0원**일 가능성이 높습니다. 그래도 카드를 등록하는 게 부담스러우시면 이 단계는 건너뛰셔도 됩니다.

### 8-1. 요금제를 Blaze로 바꾸기

1. 파이어베이스 콘솔 왼쪽 아래 **`업그레이드`** (또는 현재 요금제 `Spark`) 클릭
2. **`Blaze` 요금제 선택** → 결제 계정(카드) 연결
3. 예산 알림을 걸어두고 싶으면 안내에 따라 **예산 한도**를 설정해도 됩니다. (예: 월 1,000원)

### 8-2. Storage 켜기

1. 왼쪽 메뉴에서 **`빌드` ▸ `Storage`** 클릭
2. **`시작하기`** 클릭
3. 보안 규칙은 일단 기본값(**잠금 모드**)으로 두고 `다음`
4. 위치는 4단계에서 고른 것과 같은 **`싱가포르 (asia-southeast1)`** 을 고르면 됩니다 → `완료`
5. 화면 위쪽에 `gs://wejjoy-7f100.firebasestorage.app` 같은 주소가 보입니다. **`gs://` 를 뺀 나머지가 버킷 이름**입니다. 눈여겨 봐두세요.

### 8-3. Storage 보안 규칙 넣기

1. `Storage` 화면 위쪽 탭에서 **`Rules`** 클릭
2. 안에 있는 내용을 **전부 지우고**, 아래 내용을 **통째로 복사해서 붙여넣기**
3. **`게시`** 버튼 클릭

```
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /groups/{code}/photos/{fileName} {
      allow read: if request.auth != null;
      allow create, update: if request.auth != null
        && request.resource.size < 15 * 1024 * 1024
        && request.resource.contentType.matches('image/.*');
      allow delete: if request.auth != null;
    }
  }
}
```

> ℹ️ 참고: 5단계의 실시간 데이터베이스 규칙과 달리, Storage 규칙은 "이 사람이 진짜 그 그룹의 구성원인지"까지는 확인하지 못합니다. (파이어베이스의 구조상 Storage 규칙이 Realtime Database 안의 구성원 명단을 들여다볼 수 없습니다.) 그래서 이 규칙은 **"로그인한 사람이면 허용"** 으로 두고, 대신 그룹 코드(10억 가지 이상)와 사진 파일명(추측 불가능한 긴 문자열)이 둘 다 맞아야 접근할 수 있게 해서 실질적으로 안전하게 막습니다. 글·일정만큼 완벽하진 않지만, 초대 코드를 모르는 남이 사진에 접근할 방법은 없습니다.

### 8-4. 버킷 이름을 앱에 넣기

1. 깃허브에서 `FirebaseConfig.kt` 파일을 엽니다. (6-2와 같은 방법)
2. `STORAGE_BUCKET` 줄의 따옴표 안에 8-2에서 확인한 버킷 이름을 넣습니다.

   ```kotlin
   const val STORAGE_BUCKET = "wejjoy-7f100.firebasestorage.app"
   ```

3. `Commit changes` 로 저장하면 새 APK가 만들어집니다. (5~10분)
4. 새 APK를 **모든 구성원이 같이** 설치해야 사진 공유가 서로 잘 됩니다.

이후로는 사진을 첨부해 저장하면 자동으로 서버에 올라가고, 상대 폰에서도 같은 사진이 내려받아져 보입니다. 사진을 지우면 서버에서도 같이 지워집니다.

---

## 자주 나오는 문제

| 증상 | 원인과 해결 |
|---|---|
| "서버에 연결하지 못했습니다 (익명 로그인 설정 확인)" | **3단계 익명 로그인**을 안 켰습니다. 켜고 앱을 다시 여세요. |
| "그런 코드의 그룹이 없습니다" | 코드 오타입니다. `WJ-` 까지 포함해서 정확히 넣으세요. 대소문자는 자동으로 대문자가 됩니다. |
| "관리자가 공유를 해제한 그룹입니다" | 전에 내보내진 사람입니다. 관리자가 공유자 목록에서 **`다시 허용`** 을 눌러줘야 들어올 수 있습니다. |
| "그룹을 만들지 못했습니다: Permission denied" | **5단계 보안 규칙**이 안 들어갔습니다. 규칙을 다시 붙여넣고 `게시` 를 누르세요. |
| 설정 화면에 "아직 공유 서버가 연결되지 않았습니다" 안내가 계속 뜸 | `FirebaseConfig.kt` 의 4칸 중 빈 칸이 있습니다. 다시 확인하고 APK를 새로 받으세요. |
| 앱은 켜지는데 상대 일정이 안 옴 | 두 폰이 **같은 초대 코드**의 그룹에 있는지 확인하세요. 설정 ▸ 공유 그룹에서 코드가 같아야 합니다. |
| 사진이 상대 폰에 안 보임 | **8단계(사진도 같이 공유하기)** 를 안 하셨다면 정상입니다. 그 경우 사진은 각자 폰에만 저장되고, 글·시간·태그·중요도·기분만 공유됩니다. 8단계까지 하셨는데도 안 보이면, 두 폰 다 8단계 이후에 나온 **최신 APK**를 설치했는지 확인하세요. |

---

## 알아두면 좋은 것

- **디자인은 각자 따로입니다.** 8종(4가지 색 × 밝은/어두운) 중 각자 원하는 걸 고르면 됩니다. 내용만 똑같이 공유됩니다.
- **인터넷이 없어도** 앱은 그대로 쓸 수 있습니다. 인터넷이 연결되면 그동안 쓴 내용이 알아서 올라갑니다.
- **같은 일정을 두 사람이 동시에 고치면**, 나중에 고친 쪽 내용이 남습니다.
- 무료 요금제 용량은 **1GB 저장 / 월 10GB 전송** 입니다. 글로만 채우면 수십 년을 써도 남습니다.
- 8단계까지 해서 사진도 공유하는 경우: Blaze 요금제여도 매달 **저장 5GB / 다운로드 100GB** 까지는 무료 사용량으로 처리됩니다. 가족 단위로 쓰신다면 실제로 요금이 나올 일은 거의 없습니다.
- 파이어베이스 콘솔의 `Realtime Database ▸ 데이터` 탭에서 **실제로 저장된 내용을 눈으로 확인**할 수 있습니다.
