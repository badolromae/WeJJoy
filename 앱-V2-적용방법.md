# WeJJoy 앱 V2 — 적용 방법

## 이번 V2에서 바뀐 점
- **이모티콘 60종을 앱에 내장** (2인 20 · 남편 20 · 아내 20, 새 아기비율 · 아내 러블리 핑크)
- **일기 편집기에 이모티콘 고르기** 추가 — `대표(달력)` / `제목에` / `내용에` 를 고른 뒤 이모티콘을 누르면
  - *대표*: 그 일기의 대표 이모티콘 (목록·달력 칸에 그림으로 표시)
  - *제목에 / 내용에*: 글 속에 이모티콘을 끼워 넣기 (웹과 동일)
- **목록 카드**와 **달력 네모칸 안**에 이모티콘이 그림으로 표시
- **웹과 실시간 공유** — 앱에서 넣은 이모티콘이 웹에도, 웹에서 넣은 게 앱에도 그대로 (같은 파이어베이스 필드 `sticker`)
- **설정 화면 버전 표기: WeJJoy 버전 2.0**
- 기존 일기는 그대로 보존됩니다 (DB 자동 마이그레이션 v3→v4, `sticker` 칸만 추가)

## 들어있는 파일
```
app/build.gradle                                  (버전 2.0)
app/src/main/java/com/jooshin/diary/util/Stickers.kt          ← 새 파일
app/src/main/java/com/jooshin/diary/data/DiaryEntry.kt
app/src/main/java/com/jooshin/diary/data/AppDatabase.kt
app/src/main/java/com/jooshin/diary/sync/SyncManager.kt
app/src/main/java/com/jooshin/diary/ui/EntryEditorActivity.kt
app/src/main/java/com/jooshin/diary/ui/EntryAdapter.kt
app/src/main/java/com/jooshin/diary/ui/MonthCalendarView.kt
app/src/main/java/com/jooshin/diary/ui/MainActivity.kt
app/src/main/java/com/jooshin/diary/ui/SettingsActivity.kt
app/src/main/res/layout/item_entry.xml
app/src/main/res/layout/activity_editor.xml
app/src/main/assets/stickers/*.png                ← 새 파일 60개 (이모티콘)
```

## 적용 방법 (제일 쉬운 방법)
1. 이 압축을 풉니다. 안에 `app` 폴더가 있습니다.
2. 깃허브 `badolromae/WeJJoy` 저장소 첫 화면 ▸ **Add file ▸ Upload files**
3. 압축 푼 **`app` 폴더를 통째로 드래그**해서 올립니다.
   - 같은 경로의 기존 파일은 자동으로 새 내용으로 바뀌고, 새 파일(Stickers.kt, 이모티콘 60개)은 새로 추가됩니다.
   - (다른 앱 파일은 건드리지 않습니다 — 이 안에 든 파일만 갱신돼요)
4. 아래 **Commit changes**.
5. **Actions ▸ Build APK** 가 자동으로 돌아 새 APK(버전 2.0)를 만듭니다. 끝나면 그 실행의 **Artifacts** 에서
   `MyDiary-APK` 를 내려받아 폰에 설치하세요. **덮어쓰기(업데이트) 설치**가 되고, 기존 일기는 그대로 유지됩니다.

> ⚠️ 앱을 **지우지 마세요.** 항상 새 APK로 덮어쓰기 설치만 하세요. (지우면 그 폰의 로컬 일기가 사라집니다)

## 확인
- 일기 쓰기 화면 아래쪽에 "이모티콘 (60종)" 이 생기고, 대표/제목에/내용에 를 골라 넣을 수 있어요.
- 목록과 달력 칸에 이모티콘 그림이 뜨면 성공입니다.
- 설정 화면 맨 아래 "WeJJoy 버전 2.0" 확인.
