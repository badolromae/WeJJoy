# WeJJoy 앱 **v2.2** — 업데이트 방법 & 새 기능

앱과 웹은 이제 **같은 버전(2.2)** 입니다.

---

## 🆕 2.2 새 기능

1. **위젯 이모티콘 표시** — 주간·일간 위젯에서 이모티콘이 `[[s:couple_02_kiss]]` 같은 **글자로 나오던 문제를 고쳐**, 이제 위젯에도 이모티콘이 **그림**으로 뜨고 제목·내용의 이모티콘 글자는 깔끔하게 사라집니다.
2. **이모티콘 120종** — 기존 60종에 **아주 귀여운 강아지 20종 · 고양이 20종 · 강아지&고양이 20종**을 추가했습니다. (웹과 동일)
3. **일기 슬라이딩(스크롤) 버그 수정** — 공휴일·기념일이 있는 날 일기를 써도 **아래쪽에 안 뜨고 스크롤이 안 되던 문제**를 고쳤습니다. (달력 아래 안내줄을 한 줄로 줄이고, 목록이 안 보이면 달력이 자동으로 접히도록 처리)

(2.1 기능 그대로 포함: 일기 탭 → 전체화면 보기, 내용 속 큰 이모티콘, 연필 ✏️ 수정 / 달력 칸·제목·내용 이모티콘 / 아내 러블리 핑크 · 아기 비율)

---

## ⚠️ 가장 중요 — 기존 기록은 그대로 유지됩니다

- 이 APK는 **기존 앱 위에 그대로 덮어써서 업데이트**됩니다. **앱을 지우지 마세요.**
- 서명키(`debug-keystore.b64`)가 그대로라서 업데이트 설치가 됩니다. 만약 "기존 앱을 삭제해야 한다"는 안내가 뜨면 **설치를 멈추고** 알려주세요. (지우면 로컬 기록이 사라집니다. 단, 공유방에 올라간 일기는 파이어베이스에 있어 복구 가능)
- 데이터베이스는 기존 일기를 지우지 않고 칸만 추가하는 방식(마이그레이션)이라 안전합니다.

---

## 1. 저장소 파일 올리기

압축을 풀면 **WeJJoy 앱 저장소 전체**입니다. 깃허브 저장소에 **기존 파일 위에 덮어쓰기**로 올리면 됩니다. (핵심 변경: 이모티콘 그림 60개 추가, 버전 2.2, 위젯·스크롤·이모티콘 코드)

> 헷갈리면 그냥 압축 속 파일을 **전부** 올려 덮어쓰면 됩니다. `debug-keystore.b64` 는 반드시 그대로 있어야 합니다(서명키).

## 2. APK 만들기 (자동)

- 파일을 커밋하면 저장소 **Actions ▸ "Build APK"** 가 자동으로 돕니다.
- 초록 체크(✓)가 뜨면, 그 실행을 눌러 맨 아래 **Artifacts ▸ MyDiary-APK** 를 다운로드 → 압축을 풀면 `app-debug.apk`.

## 3. 폰에 설치

- APK를 폰으로 옮겨 실행 → **기존 WeJJoy 위에 업데이트**(덮어쓰기) 설치.
- 설치 후 **설정 화면 맨 아래에 "WeJJoy 버전 2.2"** 로 보이면 최신입니다.

---

## 참고 — 두 분(부부) 이모티콘도 그대로

앱 안 이모티콘 그림 폴더(`app/src/main/assets/stickers/`)에 120종 PNG가 들어 있어, 인터넷 없이도 바로 뜹니다. 저작권 걱정 없는 **직접 그린 오리지널** 그림입니다.
