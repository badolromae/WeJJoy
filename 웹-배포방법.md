# WeJJoy 웹 버전 **2.2** — 배포 방법 & 사용 안내

앱과 **같은 파이어베이스**(wejjoy-7f100)를 바라보므로, 앱에서 쓴 공유 일기가 웹에도 그대로 나오고, 웹에서 쓴 일기도 앱으로 넘어갑니다. 앱과 **웹은 이제 같은 버전(2.2)** 입니다.

주소: **https://wejjoy-7f100.web.app** (배포 후) — `.web.app` 주소와 https 자물쇠(SSL)는 평생 무료입니다.

---

## 🆕 2.2 새 기능

1. **넓은 화면 2단 배치** — PC·태블릿처럼 화면이 넓으면 **달력은 왼쪽, 일기는 오른쪽**으로 나뉘어 달력도 커지고 일기도 보기 편해집니다. (폰에서는 지금처럼 위아래로 보입니다.)
2. **이모티콘 120종** — 기존 60종(2인·남편·아내)에 더해 **아주 귀여운 강아지 20종 · 고양이 20종 · 강아지&고양이 20종**이 추가됐습니다.

(2.1 기능 그대로 포함: 일기 탭 → 전체화면 보기, 내용 속 큰 이모티콘, 연필 ✏️ 수정 버튼 / 달력 칸 이모티콘 / 아내 러블리 핑크 · 아기 비율)

---

## 1. 저장소에 넣을 파일

압축을 풀면 아래 구조입니다. **깃허브의 WeJJoy 저장소(앱과 같은 저장소) 맨 위**에 그대로 올리면 됩니다. 안드로이드 앱 파일(app/, build.gradle 등)과 **섞여도 전혀 문제 없습니다.**

```
firebase.json                     ← (이미 있으면 그대로 덮어쓰기)
.firebaserc                       ← (이미 있으면 그대로 덮어쓰기)
.github/workflows/deploy-web.yml  ← ⚠️ 자동 배포 파일 (아주 중요!)
public/                           ← 웹 화면 전체 (이 폴더 통째로 덮어쓰기)
  index.html  app.js  styles.css  themes.css
  date.js  lunar.js  holidays.js  firebase-config.js
  stickers/ (이모티콘 .svg 120개)
```

---

## ⚠️ 지난번에 웹이 안 뜬 이유 (꼭 확인)

지난번엔 `.github/workflows/deploy-web.yml` **자동 배포 파일이 저장소에 올라가지 않아서** 앱 APK만 만들어지고 **웹은 배포가 아예 실행되지 않았습니다.** ("Site Not Found" 화면) 이번엔 아래 2가지만 확인하면 됩니다.

### (A) `deploy-web.yml` 파일이 저장소에 실제로 있는지 확인
깃허브 저장소에서 주소창에 이렇게 쳐서 파일이 보이면 OK:
`https://github.com/badolromae/WeJJoy/blob/main/.github/workflows/deploy-web.yml`
→ **404(없음)** 가 나오면 아직 안 올라간 것입니다. 아래 2번 방법으로 올려주세요.

> 깃허브 웹 업로드는 점(.)으로 시작하는 폴더(`.github`)를 드래그하면 가끔 빠집니다. 그럴 땐 **`Add file ▸ Create new file`** 을 눌러 파일 이름 칸에 그대로
> `.github/workflows/deploy-web.yml`
> 를 치면 폴더가 자동으로 만들어집니다. 그 안에 압축 속 같은 파일 내용을 붙여넣고 커밋하세요.

### (B) 비밀값(서비스 계정)이 등록돼 있는지 확인
저장소 **Settings ▸ Secrets and variables ▸ Actions** 에 이름이
`FIREBASE_SERVICE_ACCOUNT_WEJJOY` 인 비밀값이 있어야 합니다. (없으면 아래 2번 참고)

---

## 2. 배포하기 (터미널 없이 웹에서만)

### 2-1. 서비스 계정 비밀값 만들기 (한 번만 — 이미 했으면 건너뛰기)
1. 파이어베이스 콘솔 → 톱니바퀴 **프로젝트 설정 ▸ 서비스 계정** 탭
2. **새 비공개 키 생성** → JSON 파일이 다운로드됩니다.
3. 깃허브 저장소 **Settings ▸ Secrets and variables ▸ Actions ▸ New repository secret**
   - Name: `FIREBASE_SERVICE_ACCOUNT_WEJJOY`
   - Secret: 다운로드한 **JSON 파일 내용 전체**를 열어 복사·붙여넣기
   - **Add secret**

### 2-2. 파일 올리기
저장소 첫 화면에서 **Add file ▸ Upload files** → 압축 푼 `public` 폴더와 `firebase.json`, `.firebaserc` 를 드래그. `.github/workflows/deploy-web.yml` 은 위 (A)의 방법으로 확실히 올려주세요. → **Commit changes**

### 2-3. 자동 배포 확인
- 커밋하면 저장소 **Actions** 탭에 **"Deploy WeJJoy Web"** 이 돌기 시작합니다.
- 초록 체크(✓) 가 뜨면 성공 → **https://wejjoy-7f100.web.app** 접속.
- 만약 Actions에 "Deploy WeJJoy Web" 이 **안 보이면** → `deploy-web.yml` 이 아직 저장소에 없는 것입니다. (A) 다시 확인.

> 파이어베이스 콘솔에서 **Hosting**을 아직 한 번도 시작한 적이 없다면, 콘솔 좌측 **Hosting ▸ 시작하기**를 한 번 눌러 활성화만 해두면 됩니다. (명령어 입력 필요 없음)

---

## 3. 사용 안내
- 우측 상단: 🔍 검색 · 👥 공유 · ⚙️ 설정
- 일기 추가: 우측 아래 **+**
- 이모티콘: 편집 화면에서 **대표 / 제목 / 내용** 중 넣을 곳을 고르고 아래 그림을 탭
- 일기 탭 → **전체화면 보기**, 우측 상단 **연필 ✏️** 로 수정

버전이 **설정 화면 맨 아래 "WeJJoy 버전 2.2"** 로 보이면 최신입니다.
