# WeJJoy 웹 **V3.1** — 업데이트 방법 & 변경점

앱과 웹은 **같은 버전(3.1)** 입니다. 같은 파이어베이스(wejjoy-7f100)를 공유합니다.
주소: **https://wejjoy-7f100.web.app**

---

## 🆕 V3.1 변경점 — 이모티콘 전면 교체

- **기존 이모티콘(직접 그린 60/120종)을 전부 삭제**하고, 보내주신 **새 그림 이모티콘 123종**을 등록했습니다.
- 6개 그룹으로 정리했어요:
  - **둘이A · 아내A · 남편A** — 부드러운 그림체(라벨이 위에 있는 스타일)
  - **둘이B · 아내B · 남편B** — 말풍선이 있는 라인 그림체
- 이모티콘은 원본을 **변형 없이** 하나씩 잘라 배경을 투명하게 만든 것이라, 어떤 화면 색(테마)에서도 깔끔하게 보입니다.

> **참고:** 예전 이모티콘으로 저장해 둔 일기가 있으면, 그 자리의 **이모티콘 그림만** 이제 안 보입니다(글·사진·기분은 그대로예요). 새로 고르시면 새 그림으로 다시 붙습니다.

---

## 업데이트 방법 (아주 간단)

웹은 이미 배포돼 있으니, **`public` 폴더만 이 3.1 것으로 통째로 덮어쓰면** 자동으로 반영됩니다.

1. 깃허브 **WeJJoy 저장소** → **Add file ▸ Upload files**
2. 압축을 푼 **`public` 폴더**를 통째로 드래그 → **Commit changes**
3. **Actions** 탭의 **"Deploy WeJJoy Web"** 이 초록 체크(✓) 되면 완료.
4. **https://wejjoy-7f100.web.app** 새로고침 (안 바뀌면 Ctrl+F5).

> `firebase.json`, `.firebaserc`, `.github/…/deploy-web.yml` 은 그대로 두시면 됩니다(이미 올려두셨어요).
> 새 이모티콘 그림은 `public/stickers/` 폴더 안 PNG 123장으로 들어 있어, `public` 을 통째로 올리면 함께 반영됩니다.

설정 맨 아래가 **"WeJJoy 버전 3.1"** 이면 최신입니다.
