# WeJJoy 웹 **V3.0** — 업데이트 방법 & 새 기능

앱과 웹은 **같은 버전(3.0)** 입니다. 같은 파이어베이스(wejjoy-7f100)를 보므로 앱↔웹 일기가 그대로 공유됩니다.
주소: **https://wejjoy-7f100.web.app**

---

## 🆕 V3.0 새 기능 (웹)

1. **넓은 화면 3단 배치** — 에지·크롬 등 어떤 모니터에서도 **왼쪽 절반은 달력(칸이 화면에 맞춰 커집니다)**, 오른쪽 절반은 **왼쪽=일일정(그 날 일기) · 오른쪽=주일정(그 주 7일 한눈에)** 로 나뉩니다. 창 크기가 달라져도 이 비율을 유지합니다. (폰처럼 좁은 화면에서는 달력 + 일일정만 위아래로 보입니다.)
2. **오늘로 이동 아이콘** — 달력 위 네모난 달력 모양 아이콘(📅)을 누르면 바로 오늘로 돌아옵니다.
3. **주일정에서 날짜 클릭** → 그 날이 선택되어 가운데 일일정과 달력이 함께 바뀝니다.

---

## 업데이트 방법 (이미 배포돼 있으니 아주 간단)

웹은 이미 잘 돌아가고 있으므로, **`public` 폴더만 이 V3.0 것으로 통째로 덮어쓰면** 자동으로 새 버전이 배포됩니다.

1. 깃허브 **WeJJoy 저장소** 첫 화면 → **Add file ▸ Upload files**
2. 압축을 푼 **`public` 폴더**를 통째로 드래그해서 올리기 → **Commit changes**
3. 저장소 **Actions** 탭에 **"Deploy WeJJoy Web"** 이 돌고, 초록 체크(✓)가 뜨면 완료.
4. **https://wejjoy-7f100.web.app** 에서 새로고침 (안 바뀌면 Ctrl+F5 로 강력 새로고침).

> `firebase.json`, `.firebaserc`, `.github/workflows/deploy-web.yml` 은 지난번에 이미 올리셨으니 **다시 안 올려도 됩니다.** (혹시 Actions에 "Deploy WeJJoy Web" 이 안 보이면 그때만 압축 속 `.github/workflows/deploy-web.yml` 을 다시 확인해 주세요.)

설정 화면 맨 아래가 **"WeJJoy 버전 3.0"** 이면 최신입니다.
