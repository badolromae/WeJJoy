# WeJJoy 웹 버전 — 배포 방법 & 사용 안내

앱과 **똑같은 디자인**(달력·일기 카드·8가지 색상 테마·다크모드)으로 만든 웹 버전입니다.
앱과 **같은 파이어베이스**(wejjoy-7f100)를 바라보기 때문에, 앱에서 쓴 **공유 일기가 웹에도 그대로** 나오고,
웹에서 쓴 일기도 앱으로 넘어갑니다. 두 분을 캐리커처한 **이모티콘 60종**(2인 20 · 남편 20 · 아내 20, 솔로는 달려가기·점프 등 역동적 동작)도 들어 있습니다.

주소는 배포하면 이렇게 됩니다 → **https://wejjoy-7f100.web.app** (또는 ….firebaseapp.com)
`.web.app` 주소와 https 자물쇠(SSL)는 **평생 무료**입니다.

---

## 1. 저장소에 넣을 파일

압축을 풀면 아래 구조입니다. **깃허브의 WeJJoy 저장소(앱과 같은 저장소) 맨 위**에 그대로 올리면 됩니다.

```
firebase.json                     ← 새 파일
.firebaserc                       ← 새 파일
.github/workflows/deploy-web.yml  ← 새 파일 (자동 배포용)
public/                           ← 웹 화면 전체 (이 폴더 통째로)
  index.html  app.js  styles.css  themes.css
  date.js  lunar.js  holidays.js  firebase-config.js
  stickers/ (이모티콘 20개 .svg)
```

> 안드로이드 앱 파일(app/, build.gradle 등)과 **섞여도 전혀 문제 없습니다.** 서로 건드리지 않아요.

깃허브 웹에서 폴더째 올리기가 번거로우면, `Add file ▸ Upload files` 에 압축 푼 폴더를 **드래그**해서 한 번에 올릴 수 있습니다.

---

## 2. 배포하기 (터미널 없이 웹에서만 — 권장)

컴퓨터 명령어 없이, 파이어베이스 콘솔과 깃허브 웹에서 클릭만으로 됩니다.

**(1) 파이어베이스 Hosting 켜기 (딱 한 번)**
- 파이어베이스 콘솔 ▸ 왼쪽 메뉴 **Hosting ▸ 시작하기**. 안내가 나오면 "다음"만 눌러 끝까지 진행(설치 명령은 무시해도 됩니다). 이걸 해야 `wejjoy-7f100.web.app` 주소가 만들어집니다.

**(2) 서비스 계정 키(JSON) 내려받기**
- 파이어베이스 콘솔 ▸ 왼쪽 위 **톱니바퀴 ▸ 프로젝트 설정 ▸ 서비스 계정** 탭
- **새 비공개 키 생성 ▸ 키 생성** → `.json` 파일이 하나 다운로드됩니다. (남에게 주면 안 되는 비밀 파일)

**(3) 깃허브에 비밀값으로 등록**
- 깃허브 저장소 ▸ **Settings ▸ Secrets and variables ▸ Actions ▸ New repository secret**
- Name: `FIREBASE_SERVICE_ACCOUNT_WEJJOY`
- Secret: 방금 받은 `.json` 파일을 메모장으로 열어 **내용 전체를 복사해서 붙여넣기** ▸ 저장

**(4) 파일 올리고 자동 배포**
- 1번 파일들을 저장소에 올리고 커밋하면 **Actions** 탭에서 "Deploy WeJJoy Web"이 자동으로 돕니다.
- 초록 체크가 뜨면 **https://wejjoy-7f100.web.app** 에서 열립니다. (수동 실행: Actions ▸ Deploy WeJJoy Web ▸ Run workflow)

> 익명 로그인 확인: 콘솔 ▸ Authentication ▸ 로그인 방법에 **익명(Anonymous)** 이 켜져 있어야 합니다(앱이 쓰던 거라 보통 켜져 있음).

### (참고) 컴퓨터 터미널이 익숙하면 — 더 빠른 방법
노드가 있는 컴퓨터에서 압축 푼 폴더 안에서:
```
npm install -g firebase-tools
firebase login
firebase deploy --only hosting
```

---

## 3. 로그인/사진 관련 (한 번 확인)

- **익명 로그인**: 파이어베이스 콘솔 ▸ Authentication ▸ 로그인 방법에 **익명(Anonymous)** 이 켜져 있어야 합니다.
  (앱이 이미 쓰고 있으니 보통 켜져 있습니다.)
- **사진**: 웹에서 사진 첨부까지 쓰려면 Storage(=Blaze 요금제)가 필요합니다. 글·일정·이모티콘 공유는 무료로 다 됩니다.

---

## 4. 웹 사용법

- **⚙️ 설정**: 8가지 색상 테마 + 자동/밝게/어둡게. (앱과 같은 색)
- **👥 공유**:
  - *새 공유 다이어리 만들기* — 만든 사람이 관리자(소유자)가 됩니다.
  - *초대 코드로 참여* — **앱에서 만든 그룹의 초대 코드(WJ-…)** 를 넣으면, 그 공유 일기가 웹에도 그대로 뜹니다.
    (웹은 공유 그룹의 일기를 보여주는 창입니다. 폰에서 공유 중이어야 웹에서도 보여요.)
- **＋ 버튼**: 일기 추가 — 제목·내용·날짜/시간·**기분 이모지/커플 이모티콘**·중요도·태그·사진.
- **달력**: 좌우로 넘기거나(모바일 스와이프) ‹ › 로 월 이동, 날짜를 누르면 그 날 일기가 아래에 나옵니다. 음력·공휴일도 앱과 똑같이 표시됩니다.

---

## 5. 다음 단계 (예고)

- **공유 잠금(소유자 승인제)**: 지금은 초대 코드를 아는 사람이 들어올 수 있는 방식(앱과 동일)입니다.
  다음 단계에서 **관리자(회원님) 승인 없이는 아무도 못 들어오게** 서버 규칙 + 앱 + 웹을 함께 잠급니다.
- **앱에도 커플 이모티콘 표시**: 웹에서 고른 이모티콘이 앱 목록에도 그림으로 나오도록 앱을 업데이트합니다.

두 가지는 앱까지 같이 손대야 해서, 웹이 잘 뜨는 걸 확인한 뒤 이어서 진행하겠습니다.
