[WeJJoy 앱 V3.5 — 이모티콘 흰 테두리(다이컷) 버전으로 전면 교체]
바뀐 것: 이모티콘 그림 123개(흰 테두리 추가·투명배경) + 버전 3.5.
이름은 그대로라 나머지 코드는 안 건드립니다.

압축을 풀면 app 폴더가 나옵니다. 깃허브 저장소 첫 화면에서
Add file ▸ Upload files 에 이 'app' 폴더를 통째로 드래그 → Commit.
(assets/stickers 의 png 123개와 build.gradle 만 새 것으로 덮어써집니다.)
그다음 Actions ▸ Build APK 로 새 APK 받아 기존 앱 위에 덮어써 설치.
설정 맨 아래가 'WeJJoy 버전 3.5' 이면 최신.
