# WeJJoy 앱 **V3.1** — 업데이트 방법 & 변경점

앱과 웹은 **같은 버전(3.1)** 입니다.

---

## 🆕 V3.1 변경점 — 이모티콘 전면 교체

- **기존 이모티콘을 전부 삭제**하고, 보내주신 **새 그림 이모티콘 123종**을 등록했습니다.
- 6개 그룹: **둘이A · 아내A · 남편A**(부드러운 그림체), **둘이B · 아내B · 남편B**(말풍선 라인 그림체).
- 원본을 **변형 없이** 하나씩 잘라 배경을 투명하게 만든 PNG라, 목록·달력·위젯 어디서든 깔끔합니다.
- (V3.0의 달력 좌우 스와이프 수정도 그대로 포함되어 있습니다.)

> **참고:** 예전 이모티콘으로 저장한 일기가 있으면, 그 자리의 **이모티콘 그림만** 이제 안 보입니다(글·사진·기분은 그대로). 새로 고르면 새 그림이 붙습니다. 데이터는 안전합니다.

---

## 업데이트(설치) 방법

1. 압축을 풀면 **WeJJoy 앱 저장소 전체**입니다. 깃허브 저장소에 **기존 파일 위에 덮어쓰기**로 올리세요. (`debug-keystore.b64` 는 꼭 그대로 — 서명키)
   - 새 이모티콘 그림은 `app/src/main/assets/stickers/` 폴더의 PNG 123장으로 들어 있습니다. 이 폴더가 통째로 새 파일로 바뀌어야 하니, 압축 속 파일을 **전부** 덮어써 주세요.
2. 커밋하면 **Actions ▸ "Build APK"** 가 자동으로 돕니다. 초록 체크(✓) 후 맨 아래 **Artifacts ▸ MyDiary-APK** 다운로드 → 압축 풀면 `app-debug.apk`.
3. 폰에서 실행 → **기존 WeJJoy 위에 업데이트(덮어쓰기)** 설치. **앱을 지우지 마세요.** (기록 유지)
4. 설치 후 **설정 맨 아래 "WeJJoy 버전 3.1"** 이면 최신입니다.

> "기존 앱을 삭제해야 한다"는 안내가 뜨면 설치를 멈추고 알려주세요.
